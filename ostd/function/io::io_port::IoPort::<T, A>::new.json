{
  "name": "io::io_port::IoPort::<T, A>::new",
  "safe": false,
  "callees": {
    "io::io_port::IoPort::<T, A>::new_overlapping": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Creates an I/O port.\n\n See [`IoPortAllocator::acquire`] for an explanation of the `is_overlapping` argument.\n\n [`IoPortAllocator::acquire`]: allocator::IoPortAllocator::acquire\n\n # Safety\n\n Reading from or writing to the I/O port may have side effects. Those side effects must not\n cause soundness problems (e.g., they must not corrupt the kernel memory).\n",
      "adt": {
        "io::io_port::IoPort": "Constructor"
      }
    }
  },
  "adts": {
    "io::io_port::IoPort": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "ostd::io::io_port::IoPort::<T, A>::new"
  },
  "span": "ostd/src/io/io_port/mod.rs:71:5: 74:6",
  "src": "pub(crate) const unsafe fn new(port: u16) -> Self {\n        // SAFETY: The safety is upheld by the caller.\n        unsafe { Self::new_overlapping(port, false) }\n    }",
  "mir": "fn io::io_port::IoPort::<T, A>::new(_1: u16) -> io::io_port::IoPort<T, A> {\n    let mut _0: io::io_port::IoPort<T, A>;\n    debug port => _1;\n    bb0: {\n        _0 = io::io_port::IoPort::<T, A>::new_overlapping(_1, false) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        return;\n    }\n}\n",
  "doc": " Creates an I/O port.\n\n # Safety\n\n Reading from or writing to the I/O port may have side effects. Those side effects must not\n cause soundness problems (e.g., they must not corrupt the kernel memory).\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
{
  "name": "ostd::<task::scheduler::fifo_scheduler::FifoRunQueue<T> as task::scheduler::LocalRunQueue<T>>::try_pick_next",
  "span": "ostd/src/task/scheduler/fifo_scheduler.rs:109:5: 109:51",
  "src": "fn try_pick_next(&mut self) -> Option<&Arc<T>> {\n        let next_task = self.queue.pop_front()?;\n        if let Some(prev_task) = self.current.replace(next_task) {\n            self.queue.push_back(prev_task);\n        }\n\n        self.current.as_ref()\n    }"
}
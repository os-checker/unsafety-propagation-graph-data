{
  "name": "core::num::<impl i32>::unbounded_shl",
  "span": "$library/core/src/num/int_macros.rs:1432:9: 1432:61",
  "src": "pub const fn unbounded_shl(self, rhs: u32) -> $SelfT{\n            if rhs < Self::BITS {\n                // SAFETY:\n                // rhs is just checked to be in-range above\n                unsafe { self.unchecked_shl(rhs) }\n            } else {\n                0\n            }\n        }"
}
{
  "name": "core::<iter::adapters::chain::Chain<A, B> as iter::traits::iterator::Iterator>::advance_by",
  "span": "$library/core/src/iter/adapters/chain.rs:130:5: 130:73",
  "src": "fn advance_by(&mut self, mut n: usize) -> Result<(), NonZero<usize>> {\n        if let Some(ref mut a) = self.a {\n            n = match a.advance_by(n) {\n                Ok(()) => return Ok(()),\n                Err(k) => k.get(),\n            };\n            self.a = None;\n        }\n\n        if let Some(ref mut b) = self.b {\n            return b.advance_by(n);\n            // we don't fuse the second iterator\n        }\n\n        NonZero::new(n).map_or(Ok(()), Err)\n    }"
}
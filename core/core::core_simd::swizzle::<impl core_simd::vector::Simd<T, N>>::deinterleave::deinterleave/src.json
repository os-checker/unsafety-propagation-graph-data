{
  "name": "core::core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::deinterleave::deinterleave",
  "span": "$library/core/src/../../portable-simd/crates/core_simd/src/swizzle.rs:422:9: 422:74",
  "src": "const fn deinterleave<const N: usize>(second: bool) -> [usize; N] {\n            let mut idx = [0; N];\n            let mut i = 0;\n            while i < N {\n                idx[i] = i * 2 + second as usize;\n                i += 1;\n            }\n            idx\n        }"
}
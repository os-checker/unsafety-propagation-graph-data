{
  "name": "core::<slice::iter::SplitInclusiveMut<'a, T, P> as iter::traits::iterator::Iterator>::size_hint",
  "span": "$library/core/src/slice/iter.rs:866:5: 866:50",
  "src": "fn size_hint(&self) -> (usize, Option<usize>) {\n        if self.finished {\n            (0, Some(0))\n        } else {\n            // If the predicate doesn't match anything, we yield one slice.\n            // If it matches every element, we yield `len()` one-element slices,\n            // or a single empty slice.\n            (1, Some(cmp::max(1, self.v.len())))\n        }\n    }"
}
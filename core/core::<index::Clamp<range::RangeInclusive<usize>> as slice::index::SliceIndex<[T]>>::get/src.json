{
  "name": "core::<index::Clamp<range::RangeInclusive<usize>> as slice::index::SliceIndex<[T]>>::get",
  "span": "$library/core/src/index.rs:173:5: 173:55",
  "src": "fn get(self, slice: &[T]) -> Option<&Self::Output> {\n        let start = cmp::min(self.0.start, slice.len() - 1);\n        let end = cmp::min(self.0.last, slice.len() - 1);\n        (start..=end).get(slice)\n    }"
}
{
  "name": "core::<core_simd::vector::Simd<i32, N> as core_simd::simd::cmp::ord::SimdOrd>::simd_clamp",
  "span": "$library/core/src/../../portable-simd/crates/core_simd/src/simd/cmp/ord.rs:100:13: 100:62",
  "src": "fn simd_clamp(self, min: Self, max: Self) -> Self {\n                assert!(\n                    min.simd_le(max).all(),\n                    \"each element in `min` must be less than or equal to the corresponding element in `max`\",\n                );\n                self.simd_max(min).simd_min(max)\n            }"
}
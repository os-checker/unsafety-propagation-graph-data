{
  "name": "core::<index::Clamp<ops::range::Range<usize>> as slice::index::SliceIndex<[T]>>::get",
  "span": "$library/core/src/index.rs:130:5: 130:55",
  "src": "fn get(self, slice: &[T]) -> Option<&Self::Output> {\n        let start = cmp::min(self.0.start, slice.len());\n        let end = cmp::min(self.0.end, slice.len());\n        (start..end).get(slice)\n    }"
}
{
  "name": "cmp::Ordering::reverse",
  "safe": true,
  "callees": {},
  "adts": {
    "cmp::Ordering": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "core::cmp::Ordering::reverse"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/cmp.rs:562:5: 568:6",
  "src": "pub const fn reverse(self) -> Ordering {\n        match self {\n            Less => Greater,\n            Equal => Equal,\n            Greater => Less,\n        }\n    }",
  "mir": "fn cmp::Ordering::reverse(_1: cmp::Ordering) -> cmp::Ordering {\n    let mut _0: cmp::Ordering;\n    let mut _2: i8;\n    debug self => _1;\n    bb0: {\n        _2 = discriminant(_1);\n        switchInt(move _2) -> [255: bb4, 0: bb3, 1: bb2, otherwise: bb1];\n    }\n    bb1: {\n        unreachable;\n    }\n    bb2: {\n        _0 = cmp::Ordering::Less;\n        goto -> bb5;\n    }\n    bb3: {\n        _0 = cmp::Ordering::Equal;\n        goto -> bb5;\n    }\n    bb4: {\n        _0 = cmp::Ordering::Greater;\n        goto -> bb5;\n    }\n    bb5: {\n        return;\n    }\n}\n",
  "doc": " Reverses the `Ordering`.\n\n * `Less` becomes `Greater`.\n * `Greater` becomes `Less`.\n * `Equal` becomes `Equal`.\n\n # Examples\n\n Basic behavior:\n\n ```\n use std::cmp::Ordering;\n\n assert_eq!(Ordering::Less.reverse(), Ordering::Greater);\n assert_eq!(Ordering::Equal.reverse(), Ordering::Equal);\n assert_eq!(Ordering::Greater.reverse(), Ordering::Less);\n ```\n\n This method can be used to reverse a comparison:\n\n ```\n let data: &mut [_] = &mut [2, 10, 5, 8];\n\n // sort the array from largest to smallest.\n data.sort_by(|a, b| a.cmp(b).reverse());\n\n let b: &mut [_] = &mut [10, 8, 5, 2];\n assert!(data == b);\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
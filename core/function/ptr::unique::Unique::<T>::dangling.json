{
  "name": "ptr::unique::Unique::<T>::dangling",
  "safe": true,
  "callees": {
    "ptr::non_null::NonNull::<T>::dangling": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Creates a new `NonNull` that is dangling, but well-aligned.\n\n This is useful for initializing types which lazily allocate, like\n `Vec::new` does.\n\n Note that the address of the returned pointer may potentially\n be that of a valid pointer, which means this must not be used\n as a \"not yet initialized\" sentinel value.\n Types that lazily allocate must track initialization by some other means.\n\n # Examples\n\n ```\n use std::ptr::NonNull;\n\n let ptr = NonNull::<u32>::dangling();\n // Important: don't try to access the value of `ptr` without\n // initializing it first! The pointer is not null but isn't valid either!\n ```\n",
      "adt": {
        "ptr::non_null::NonNull": "Constructor"
      }
    }
  },
  "adts": {
    "ptr::non_null::NonNull": [
      "Plain"
    ],
    "ptr::unique::Unique": [
      "Plain"
    ]
  },
  "path": 16058,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/ptr/unique.rs:73:5: 76:6",
  "src": "pub const fn dangling() -> Self {\n        // FIXME(const-hack) replace with `From`\n        Unique { pointer: NonNull::dangling(), _marker: PhantomData }\n    }",
  "mir": "fn ptr::unique::Unique::<T>::dangling() -> ptr::unique::Unique<T> {\n    let mut _0: ptr::unique::Unique<T>;\n    let mut _1: ptr::non_null::NonNull<T>;\n    bb0: {\n        StorageLive(_1);\n        _1 = ptr::non_null::NonNull::<T>::dangling() -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _0 = Unique(move _1, ZeroSized: marker::PhantomData<T>);\n        StorageDead(_1);\n        return;\n    }\n}\n",
  "doc": " Creates a new `Unique` that is dangling, but well-aligned.\n\n This is useful for initializing types which lazily allocate, like\n `Vec::new` does.\n\n Note that the address of the returned pointer may potentially\n be that of a valid pointer, which means this must not be used\n as a \"not yet initialized\" sentinel value.\n Types that lazily allocate must track initialization by some other means.\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
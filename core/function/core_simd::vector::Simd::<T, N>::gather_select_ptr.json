{
  "name": "core_simd::vector::Simd::<T, N>::gather_select_ptr",
  "safe": false,
  "callees": {
    "core_simd::masks::Mask::<T, N>::to_int": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Converts the mask to a vector of integers, where 0 represents `false` and -1\n represents `true`.\n",
      "adt": {
        "core_simd::vector::Simd": "Constructor"
      }
    },
    "intrinsics::simd::simd_gather": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Reads a vector of pointers.\n\n `T` must be a vector.\n\n `U` must be a vector of pointers to the element type of `T`, with the same length as `T`.\n\n `V` must be a vector of integers with the same length as `T` (but any element size).\n\n For each pointer in `ptr`, if the corresponding value in `mask` is `!0`, read the pointer.\n Otherwise if the corresponding value in `mask` is `0`, return the corresponding value from\n `val`.\n\n # Safety\n Unmasked values in `T` must be readable as if by `<ptr>::read` (e.g. aligned to the element\n type).\n\n `mask` must only contain `0` or `!0` values.\n",
      "adt": {}
    }
  },
  "adts": {
    "core_simd::masks::Mask": [
      "Plain"
    ],
    "core_simd::vector::Simd": [
      "Plain"
    ]
  },
  "path": 12672,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/../../portable-simd/crates/core_simd/src/vector.rs:656:5: 663:6",
  "src": "pub unsafe fn gather_select_ptr(\n        source: Simd<*const T, N>,\n        enable: Mask<isize, N>,\n        or: Self,\n    ) -> Self {\n        // Safety: The caller is responsible for upholding all invariants\n        unsafe { core::intrinsics::simd::simd_gather(or, source, enable.to_int()) }\n    }",
  "mir": "fn core_simd::vector::Simd::<T, N>::gather_select_ptr(_1: core_simd::vector::Simd<*const T, N>, _2: core_simd::masks::Mask<isize, N>, _3: core_simd::vector::Simd<T, N>) -> core_simd::vector::Simd<T, N> {\n    let mut _0: core_simd::vector::Simd<T, N>;\n    let mut _4: core_simd::vector::Simd<isize, N>;\n    debug source => _1;\n    debug enable => _2;\n    debug or => _3;\n    bb0: {\n        StorageLive(_4);\n        _4 = core_simd::masks::Mask::<isize, N>::to_int(_2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _0 = intrinsics::simd::simd_gather::<core_simd::vector::Simd<T, N>, core_simd::vector::Simd<*const T, N>, core_simd::vector::Simd<isize, N>>(_3, _1, move _4) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        StorageDead(_4);\n        return;\n    }\n}\n",
  "doc": " Conditionally read elementwise from pointers into a SIMD vector.\n The mask `enable`s all `true` pointers and disables all `false` pointers.\n If a pointer is disabled, the element is selected from the `or` vector,\n and no read is performed.\n\n # Safety\n\n Enabled elements must satisfy the same conditions as [`core::ptr::read`].\n\n # Example\n ```\n # #![feature(portable_simd)]\n # #[cfg(feature = \"as_crate\")] use core_simd::simd;\n # #[cfg(not(feature = \"as_crate\"))] use core::simd;\n # use simd::prelude::*;\n let values = [6, 2, 4, 9];\n let enable = Mask::from_array([true, true, false, true]);\n let offsets = Simd::from_array([1, 0, 0, 3]);\n let source = Simd::splat(values.as_ptr()).wrapping_add(offsets);\n let gathered = unsafe { Simd::gather_select_ptr(source, enable, Simd::splat(0)) };\n assert_eq!(gathered, Simd::from_array([2, 6, 0, 9]));\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
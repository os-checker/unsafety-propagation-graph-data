{
  "name": "core_simd::swizzle::Swizzle::concat_swizzle_mask",
  "safe": true,
  "callees": {
    "core_simd::masks::Mask::<T, N>::to_int": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Converts the mask to a vector of integers, where 0 represents `false` and -1\n represents `true`.\n",
      "adt": {
        "core_simd::vector::Simd": "Constructor"
      }
    },
    "core_simd::swizzle::Swizzle::concat_swizzle": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Creates a new vector from the elements of `first` and `second`.\n\n Lane `i` of the output is `concat[Self::INDEX[i]]`, where `concat` is the concatenation of\n `first` and `second`.\n",
      "adt": {
        "core_simd::vector::Simd": "Constructor"
      }
    },
    "core_simd::masks::Mask::<T, N>::from_int_unchecked": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Converts a vector of integers to a mask, where 0 represents `false` and -1\n represents `true`.\n\n # Safety\n All elements must be either 0 or -1.\n",
      "adt": {
        "core_simd::masks::Mask": "Constructor"
      }
    }
  },
  "adts": {
    "core_simd::masks::Mask": [
      "Plain"
    ],
    "core_simd::vector::Simd": [
      "Plain"
    ]
  },
  "path": {
    "type": "External",
    "path": "core_simd::swizzle::Swizzle::concat_swizzle_mask"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/../../portable-simd/crates/core_simd/src/swizzle.rs:177:5: 185:6",
  "src": "fn concat_swizzle_mask<T, const M: usize>(first: Mask<T, M>, second: Mask<T, M>) -> Mask<T, N>\n    where\n        T: MaskElement,\n        LaneCount<N>: SupportedLaneCount,\n        LaneCount<M>: SupportedLaneCount,\n    {\n        // SAFETY: all elements of this mask come from another mask\n        unsafe { Mask::from_int_unchecked(Self::concat_swizzle(first.to_int(), second.to_int())) }\n    }",
  "mir": "fn core_simd::swizzle::Swizzle::concat_swizzle_mask(_1: core_simd::masks::Mask<T, M>, _2: core_simd::masks::Mask<T, M>) -> core_simd::masks::Mask<T, N> {\n    let mut _0: core_simd::masks::Mask<T, N>;\n    let mut _3: core_simd::vector::Simd<T, N>;\n    let mut _4: core_simd::vector::Simd<T, M>;\n    let mut _5: core_simd::vector::Simd<T, M>;\n    debug first => _1;\n    debug second => _2;\n    bb0: {\n        StorageLive(_3);\n        StorageLive(_4);\n        _4 = core_simd::masks::Mask::<T, M>::to_int(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        StorageLive(_5);\n        _5 = core_simd::masks::Mask::<T, M>::to_int(_2) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        _3 = <Self as core_simd::swizzle::Swizzle<N>>::concat_swizzle::<T, M>(move _4, move _5) -> [return: bb3, unwind unreachable];\n    }\n    bb3: {\n        StorageDead(_5);\n        StorageDead(_4);\n        _0 = core_simd::masks::Mask::<T, N>::from_int_unchecked(move _3) -> [return: bb4, unwind unreachable];\n    }\n    bb4: {\n        StorageDead(_3);\n        return;\n    }\n}\n",
  "doc": " Creates a new mask from the elements of `first` and `second`.\n\n Element `i` of the output is `concat[Self::INDEX[i]]`, where `concat` is the concatenation of\n `first` and `second`.\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
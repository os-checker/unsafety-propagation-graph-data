{
  "name": "core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::interleave",
  "safe": true,
  "callees": {
    "core_simd::swizzle::Swizzle::concat_swizzle": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Creates a new vector from the elements of `first` and `second`.\n\n Lane `i` of the output is `concat[Self::INDEX[i]]`, where `concat` is the concatenation of\n `first` and `second`.\n",
      "adt": {
        "core_simd::vector::Simd": "Constructor"
      }
    }
  },
  "adts": {
    "core_simd::vector::Simd": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "core::core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::interleave"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/../../portable-simd/crates/core_simd/src/swizzle.rs:368:5: 396:6",
  "src": "pub fn interleave(self, other: Self) -> (Self, Self) {\n        const fn interleave<const N: usize>(high: bool) -> [usize; N] {\n            let mut idx = [0; N];\n            let mut i = 0;\n            while i < N {\n                let dst_index = if high { i + N } else { i };\n                let src_index = dst_index / 2 + (dst_index % 2) * N;\n                idx[i] = src_index;\n                i += 1;\n            }\n            idx\n        }\n\n        struct Lo;\n        struct Hi;\n\n        impl<const N: usize> Swizzle<N> for Lo {\n            const INDEX: [usize; N] = interleave::<N>(false);\n        }\n\n        impl<const N: usize> Swizzle<N> for Hi {\n            const INDEX: [usize; N] = interleave::<N>(true);\n        }\n\n        (\n            Lo::concat_swizzle(self, other),\n            Hi::concat_swizzle(self, other),\n        )\n    }",
  "mir": "fn core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::interleave(_1: core_simd::vector::Simd<T, N>, _2: core_simd::vector::Simd<T, N>) -> (core_simd::vector::Simd<T, N>, core_simd::vector::Simd<T, N>) {\n    let mut _0: (core_simd::vector::Simd<T, N>, core_simd::vector::Simd<T, N>);\n    let mut _3: core_simd::vector::Simd<T, N>;\n    let mut _4: core_simd::vector::Simd<T, N>;\n    debug self => _1;\n    debug other => _2;\n    bb0: {\n        StorageLive(_3);\n        _3 = <core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::interleave::Lo as core_simd::swizzle::Swizzle<N>>::concat_swizzle::<T, N>(_1, _2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        StorageLive(_4);\n        _4 = <core_simd::swizzle::<impl core_simd::vector::Simd<T, N>>::interleave::Hi as core_simd::swizzle::Swizzle<N>>::concat_swizzle::<T, N>(_1, _2) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        _0 = (move _3, move _4);\n        StorageDead(_4);\n        StorageDead(_3);\n        return;\n    }\n}\n",
  "doc": " Interleave two vectors.\n\n The resulting vectors contain elements taken alternatively from `self` and `other`, first\n filling the first result, and then the second.\n\n The reverse of this operation is [`Simd::deinterleave`].\n\n ```\n # #![feature(portable_simd)]\n # use core::simd::Simd;\n let a = Simd::from_array([0, 1, 2, 3]);\n let b = Simd::from_array([4, 5, 6, 7]);\n let (x, y) = a.interleave(b);\n assert_eq!(x.to_array(), [0, 4, 1, 5]);\n assert_eq!(y.to_array(), [2, 6, 3, 7]);\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
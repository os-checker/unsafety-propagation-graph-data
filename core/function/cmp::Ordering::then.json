{
  "name": "cmp::Ordering::then",
  "safe": true,
  "callees": {},
  "adts": {
    "cmp::Ordering": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "core::cmp::Ordering::then"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/cmp.rs:601:5: 606:6",
  "src": "pub const fn then(self, other: Ordering) -> Ordering {\n        match self {\n            Equal => other,\n            _ => self,\n        }\n    }",
  "mir": "fn cmp::Ordering::then(_1: cmp::Ordering, _2: cmp::Ordering) -> cmp::Ordering {\n    let mut _0: cmp::Ordering;\n    let mut _3: i8;\n    debug self => _1;\n    debug other => _2;\n    bb0: {\n        _3 = discriminant(_1);\n        switchInt(move _3) -> [0: bb2, otherwise: bb1];\n    }\n    bb1: {\n        _0 = _1;\n        goto -> bb3;\n    }\n    bb2: {\n        _0 = _2;\n        goto -> bb3;\n    }\n    bb3: {\n        return;\n    }\n}\n",
  "doc": " Chains two orderings.\n\n Returns `self` when it's not `Equal`. Otherwise returns `other`.\n\n # Examples\n\n ```\n use std::cmp::Ordering;\n\n let result = Ordering::Equal.then(Ordering::Less);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Less.then(Ordering::Equal);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Less.then(Ordering::Greater);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Equal.then(Ordering::Equal);\n assert_eq!(result, Ordering::Equal);\n\n let x: (i64, i64, i64) = (1, 2, 7);\n let y: (i64, i64, i64) = (1, 5, 3);\n let result = x.0.cmp(&y.0).then(x.1.cmp(&y.1)).then(x.2.cmp(&y.2));\n\n assert_eq!(result, Ordering::Less);\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
{
  "name": "<&A as alloc::Allocator>::allocate_zeroed",
  "safe": true,
  "callees": {
    "alloc::Allocator::allocate_zeroed": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Behaves like `allocate`, but also ensures that the returned memory is zero-initialized.\n\n # Errors\n\n Returning `Err` indicates that either memory is exhausted or `layout` does not meet\n allocator's size or alignment constraints.\n\n Implementations are encouraged to return `Err` on memory exhaustion rather than panicking or\n aborting, but this is not a strict requirement. (Specifically: it is *legal* to implement\n this trait atop an underlying native allocation library that aborts on memory exhaustion.)\n\n Clients wishing to abort computation in response to an allocation error are encouraged to\n call the [`handle_alloc_error`] function, rather than directly invoking `panic!` or similar.\n\n [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html\n",
      "adt": {
        "result::Result": "Constructor"
      }
    }
  },
  "adts": {
    "alloc::layout::Layout": [
      "Plain"
    ],
    "result::Result": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "core::<&A as alloc::Allocator>::allocate_zeroed"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/alloc/mod.rs:381:5: 383:6",
  "src": "fn allocate_zeroed(&self, layout: Layout) -> Result<NonNull<[u8]>, AllocError> {\n        (**self).allocate_zeroed(layout)\n    }",
  "mir": "fn <&A as alloc::Allocator>::allocate_zeroed(_1: &&A, _2: alloc::layout::Layout) -> result::Result<ptr::non_null::NonNull<[u8]>, alloc::AllocError> {\n    let mut _0: result::Result<ptr::non_null::NonNull<[u8]>, alloc::AllocError>;\n    let mut _3: &A;\n    debug self => _1;\n    debug layout => _2;\n    bb0: {\n        _3 = (*_1);\n        _0 = <A as alloc::Allocator>::allocate_zeroed(_3, _2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        return;\n    }\n}\n",
  "doc": "",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
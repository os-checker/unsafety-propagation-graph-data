{
  "name": "num::<impl usize>::borrowing_sub",
  "safe": true,
  "callees": {
    "num::<impl usize>::overflowing_sub": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Calculates `self` - `rhs`.\n\n Returns a tuple of the subtraction along with a boolean indicating\n whether an arithmetic overflow would occur. If an overflow would\n have occurred then the wrapped value is returned.\n\n # Examples\n\n ```\n ```\n",
      "adt": {}
    },
    "intrinsics::disjoint_bitor": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Combine two values which have no bits in common.\n\n This allows the backend to implement it as `a + b` *or* `a | b`,\n depending which is easier to implement on a specific target.\n\n # Safety\n\n Requires that `(a & b) == 0`, or equivalently that `(a | b) == (a + b)`.\n\n Otherwise it's immediate UB.\n",
      "adt": {}
    }
  },
  "adts": {},
  "path": 4291,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/num/uint_macros.rs:2860:9: 2870:10",
  "src": "pub const fn borrowing_sub(self, rhs: Self, borrow: bool) -> (Self, bool) {\n            // note: longer-term this should be done via an intrinsic, but this has been shown\n            //   to generate optimal code for now, and LLVM doesn't have an equivalent intrinsic\n            let (a, c1) = self.overflowing_sub(rhs);\n            let (b, c2) = a.overflowing_sub(borrow as $SelfT);\n            // SAFETY: Only one of `c1` and `c2` can be set.\n            // For c1 to be set we need to have underflowed, but if we did then\n            // `a` is nonzero, which means that `c2` cannot possibly\n            // underflow because it's subtracting at most `1` (since it came from `bool`)\n            (b, unsafe { intrinsics::disjoint_bitor(c1, c2) })\n        }",
  "mir": "fn num::<impl usize>::borrowing_sub(_1: usize, _2: usize, _3: bool) -> (usize, bool) {\n    let mut _0: (usize, bool);\n    let  _4: usize;\n    let  _5: bool;\n    let mut _6: (usize, bool);\n    let  _7: usize;\n    let  _8: bool;\n    let mut _9: (usize, bool);\n    let mut _10: usize;\n    let mut _11: bool;\n    debug self => _1;\n    debug rhs => _2;\n    debug borrow => _3;\n    debug a => _4;\n    debug c1 => _5;\n    debug b => _7;\n    debug c2 => _8;\n    bb0: {\n        StorageLive(_6);\n        _6 = num::<impl usize>::overflowing_sub(_1, _2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _4 = (_6.0: usize);\n        _5 = (_6.1: bool);\n        StorageDead(_6);\n        StorageLive(_9);\n        StorageLive(_10);\n        _10 = _3 as usize;\n        _9 = num::<impl usize>::overflowing_sub(_4, move _10) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        StorageDead(_10);\n        _7 = (_9.0: usize);\n        _8 = (_9.1: bool);\n        StorageDead(_9);\n        StorageLive(_11);\n        _11 = intrinsics::disjoint_bitor::<bool>(_5, _8) -> [return: bb3, unwind unreachable];\n    }\n    bb3: {\n        _0 = (_7, move _11);\n        StorageDead(_11);\n        return;\n    }\n}\n",
  "doc": " Calculates `self` &minus; `rhs` &minus; `borrow` and returns a tuple\n containing the difference and the output borrow.\n\n Performs \"ternary subtraction\" by subtracting both an integer\n operand and a borrow-in bit from `self`, and returns an output\n integer and a borrow-out bit. This allows chaining together multiple\n subtractions to create a wider subtraction, and can be useful for\n bignum subtraction.\n\n # Examples\n\n ```\n // ---------\n\n let borrow0 = false;\n\n let (diff0, borrow1) = a0.borrowing_sub(b0, borrow0);\n assert_eq!(borrow1, true);\n let (diff1, borrow2) = a1.borrowing_sub(b1, borrow1);\n assert_eq!(borrow2, false);\n\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
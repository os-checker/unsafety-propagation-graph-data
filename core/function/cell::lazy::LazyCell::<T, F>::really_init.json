{
  "name": "cell::lazy::LazyCell::<T, F>::really_init",
  "safe": false,
  "callees": {
    "cell::UnsafeCell::<T>::get": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Gets a mutable pointer to the wrapped value.\n\n This can be cast to a pointer of any kind. When creating references, you must uphold the\n aliasing rules; see [the type-level docs][UnsafeCell#aliasing-rules] for more discussion and\n caveats.\n\n # Examples\n\n ```\n use std::cell::UnsafeCell;\n\n let uc = UnsafeCell::new(5);\n\n let five = uc.get();\n ```\n",
      "adt": {
        "cell::UnsafeCell": "MutableAsArgument"
      }
    },
    "mem::replace": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Moves `src` into the referenced `dest`, returning the previous `dest` value.\n\n Neither value is dropped.\n\n * If you want to replace the values of two variables, see [`swap`].\n * If you want to replace with a default value, see [`take`].\n\n # Examples\n\n A simple example:\n\n ```\n use std::mem;\n\n let mut v: Vec<i32> = vec![1, 2];\n\n let old_v = mem::replace(&mut v, vec![3, 4, 5]);\n assert_eq!(vec![1, 2], old_v);\n assert_eq!(vec![3, 4, 5], v);\n ```\n\n `replace` allows consumption of a struct field by replacing it with another value.\n Without `replace` you can run into issues like these:\n\n ```compile_fail,E0507\n struct Buffer<T> { buf: Vec<T> }\n\n impl<T> Buffer<T> {\n     fn replace_index(&mut self, i: usize, v: T) -> T {\n         // error: cannot move out of dereference of `&mut`-pointer\n         let t = self.buf[i];\n         self.buf[i] = v;\n         t\n     }\n }\n ```\n\n Note that `T` does not necessarily implement [`Clone`], so we can't even clone `self.buf[i]` to\n avoid the move. But `replace` can be used to disassociate the original value at that index from\n `self`, allowing it to be returned:\n\n ```\n # #![allow(dead_code)]\n use std::mem;\n\n # struct Buffer<T> { buf: Vec<T> }\n impl<T> Buffer<T> {\n     fn replace_index(&mut self, i: usize, v: T) -> T {\n         mem::replace(&mut self.buf[i], v)\n     }\n }\n\n let mut buffer = Buffer { buf: vec![0, 1] };\n assert_eq!(buffer.buf[0], 0);\n\n assert_eq!(buffer.replace_index(0, 2), 0);\n assert_eq!(buffer.buf[0], 2);\n ```\n",
      "adt": {}
    },
    "ptr::mut_ptr::<impl *mut T>::write": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Overwrites a memory location with the given value without reading or\n dropping the old value.\n\n See [`ptr::write`] for safety concerns and examples.\n\n [`ptr::write`]: crate::ptr::write()\n",
      "adt": {}
    },
    "panicking::panic": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " The underlying implementation of core's `panic!` macro when no formatting is used.\n",
      "adt": {}
    },
    "ops::function::FnOnce::call_once": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Performs the call operation.\n",
      "adt": {}
    }
  },
  "adts": {
    "cell::UnsafeCell": [
      "Ref"
    ],
    "cell::lazy::LazyCell": [
      "DerefVariantField(VariantIdx(None)-FieldIdx(Some(0)))",
      "Ref"
    ],
    "cell::lazy::State": [
      "MutRef",
      "Plain",
      "Unknown([Downcast(VariantIdx(0, ThreadLocalIndex)), Field(0, Ty { id: 1515, kind: Param(ParamTy { index: 1, name: \"F\" }) })])",
      "Ref",
      "Deref",
      "Unknown([Deref, Downcast(VariantIdx(1, ThreadLocalIndex)), Field(0, Ty { id: 95, kind: Param(ParamTy { index: 0, name: \"T\" }) })])"
    ]
  },
  "path": 4694,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/cell/lazy.rs:230:5: 257:6",
  "src": "unsafe fn really_init(this: &LazyCell<T, F>) -> &T {\n        // SAFETY:\n        // This function is only called when the state is uninitialized,\n        // so no references to `state` can exist except for the reference\n        // in `force`, which is invalidated here and not accessed again.\n        let state = unsafe { &mut *this.state.get() };\n        // Temporarily mark the state as poisoned. This prevents reentrant\n        // accesses and correctly poisons the cell if the closure panicked.\n        let State::Uninit(f) = mem::replace(state, State::Poisoned) else { unreachable!() };\n\n        let data = f();\n\n        // SAFETY:\n        // If the closure accessed the cell through something like a reentrant\n        // mutex, but caught the panic resulting from the state being poisoned,\n        // the mutable borrow for `state` will be invalidated, so we need to\n        // go through the `UnsafeCell` pointer here. The state can only be\n        // poisoned at this point, so using `write` to skip the destructor\n        // of `State` should help the optimizer.\n        unsafe { this.state.get().write(State::Init(data)) };\n\n        // SAFETY:\n        // The previous references were invalidated by the `write` call above,\n        // so do a new shared borrow of the state instead.\n        let state = unsafe { &*this.state.get() };\n        let State::Init(data) = state else { unreachable!() };\n        data\n    }",
  "mir": "fn cell::lazy::LazyCell::<T, F>::really_init(_1: &cell::lazy::LazyCell<T, F>) -> &T {\n    let mut _0: &T;\n    let mut _2: &mut cell::lazy::State<T, F>;\n    let mut _3: *mut cell::lazy::State<T, F>;\n    let mut _4: &cell::UnsafeCell<cell::lazy::State<T, F>>;\n    let mut _5: !;\n    let  _6: F;\n    let mut _7: cell::lazy::State<T, F>;\n    let mut _8: cell::lazy::State<T, F>;\n    let mut _9: isize;\n    let  _10: T;\n    let  _11: ();\n    let mut _12: *mut cell::lazy::State<T, F>;\n    let mut _13: &cell::UnsafeCell<cell::lazy::State<T, F>>;\n    let mut _14: cell::lazy::State<T, F>;\n    let  _15: &cell::lazy::State<T, F>;\n    let  _16: *mut cell::lazy::State<T, F>;\n    let mut _17: &cell::UnsafeCell<cell::lazy::State<T, F>>;\n    let mut _18: !;\n    let mut _19: isize;\n    let mut _20: isize;\n    let mut _21: isize;\n    debug this => _1;\n    debug state => _2;\n    debug f => _6;\n    debug data => _10;\n    debug state => _15;\n    debug data => _0;\n    bb0: {\n        StorageLive(_3);\n        StorageLive(_4);\n        _4 = &((*_1).0: cell::UnsafeCell<cell::lazy::State<T, F>>);\n        _3 = cell::UnsafeCell::<cell::lazy::State<T, F>>::get(move _4) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        StorageDead(_4);\n        _2 = &mut (*_3);\n        StorageLive(_7);\n        StorageLive(_8);\n        _8 = cell::lazy::State::Poisoned;\n        _7 = mem::replace::<cell::lazy::State<T, F>>(_2, move _8) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        StorageDead(_8);\n        _9 = discriminant(_7);\n        switchInt(move _9) -> [0: bb3, otherwise: bb14];\n    }\n    bb3: {\n        _6 = move ((_7 as variant#0).0: F);\n        _20 = discriminant(_7);\n        switchInt(move _20) -> [0: bb10, otherwise: bb11];\n    }\n    bb4: {\n        StorageLive(_12);\n        StorageLive(_13);\n        _13 = &((*_1).0: cell::UnsafeCell<cell::lazy::State<T, F>>);\n        _12 = cell::UnsafeCell::<cell::lazy::State<T, F>>::get(move _13) -> [return: bb5, unwind unreachable];\n    }\n    bb5: {\n        StorageDead(_13);\n        StorageLive(_14);\n        _14 = cell::lazy::State::Init(_10);\n        _11 = ptr::mut_ptr::<impl *mut cell::lazy::State<T, F>>::write(move _12, move _14) -> [return: bb6, unwind unreachable];\n    }\n    bb6: {\n        StorageDead(_14);\n        StorageDead(_12);\n        StorageLive(_15);\n        StorageLive(_16);\n        StorageLive(_17);\n        _17 = &((*_1).0: cell::UnsafeCell<cell::lazy::State<T, F>>);\n        _16 = cell::UnsafeCell::<cell::lazy::State<T, F>>::get(move _17) -> [return: bb7, unwind unreachable];\n    }\n    bb7: {\n        StorageDead(_17);\n        _15 = &(*_16);\n        _19 = discriminant((*_15));\n        switchInt(move _19) -> [1: bb9, otherwise: bb8];\n    }\n    bb8: {\n        _18 = panicking::panic(\"internal error: entered unreachable code\") -> unwind unreachable;\n    }\n    bb9: {\n        _0 = &(((*_15) as variant#1).0: T);\n        StorageDead(_16);\n        StorageDead(_15);\n        StorageDead(_3);\n        return;\n    }\n    bb10: {\n        StorageDead(_7);\n        _10 = <F as ops::function::FnOnce<()>>::call_once(_6, ()) -> [return: bb4, unwind unreachable];\n    }\n    bb11: {\n        drop(_7) -> [return: bb10, unwind unreachable];\n    }\n    bb12: {\n        StorageDead(_7);\n        _5 = panicking::panic(\"internal error: entered unreachable code\") -> unwind unreachable;\n    }\n    bb13: {\n        drop(_7) -> [return: bb12, unwind unreachable];\n    }\n    bb14: {\n        _21 = discriminant(_7);\n        switchInt(move _21) -> [0: bb12, otherwise: bb13];\n    }\n}\n",
  "doc": " # Safety\n May only be called when the state is `Uninit`.\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
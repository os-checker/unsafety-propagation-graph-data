{
  "name": "core_simd::vector::Simd::<T, N>::load_select_unchecked",
  "safe": false,
  "callees": {
    "slice::<impl [T]>::as_ptr": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Returns a raw pointer to the slice's buffer.\n\n The caller must ensure that the slice outlives the pointer this\n function returns, or else it will end up dangling.\n\n The caller must also ensure that the memory the pointer (non-transitively) points to\n is never written to (except inside an `UnsafeCell`) using this pointer or any pointer\n derived from it. If you need to mutate the contents of the slice, use [`as_mut_ptr`].\n\n Modifying the container referenced by this slice may cause its buffer\n to be reallocated, which would also make any pointers to it invalid.\n\n # Examples\n\n ```\n let x = &[1, 2, 4];\n let x_ptr = x.as_ptr();\n\n unsafe {\n     for i in 0..x.len() {\n         assert_eq!(x.get_unchecked(i), &*x_ptr.add(i));\n     }\n }\n ```\n\n [`as_mut_ptr`]: slice::as_mut_ptr\n",
      "adt": {}
    },
    "core_simd::vector::Simd::<T, N>::load_select_ptr": {
      "safe": false,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Reads contiguous elements starting at `ptr`. Each element is read from memory if its\n corresponding element in `enable` is `true`.\n\n When the element is disabled, that memory location is not accessed and the corresponding\n value from `or` is passed through.\n\n # Safety\n Enabled `ptr` elements must be safe to read as if by `std::ptr::read`.\n",
      "adt": {
        "core_simd::vector::Simd": "Constructor"
      }
    }
  },
  "adts": {
    "core_simd::masks::Mask": [
      "Plain"
    ],
    "core_simd::vector::Simd": [
      "Plain"
    ]
  },
  "path": 12682,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/../../portable-simd/crates/core_simd/src/vector.rs:451:5: 459:6",
  "src": "pub unsafe fn load_select_unchecked(\n        slice: &[T],\n        enable: Mask<<T as SimdElement>::Mask, N>,\n        or: Self,\n    ) -> Self {\n        let ptr = slice.as_ptr();\n        // SAFETY: The safety of reading elements from `slice` is ensured by the caller.\n        unsafe { Self::load_select_ptr(ptr, enable, or) }\n    }",
  "mir": "fn core_simd::vector::Simd::<T, N>::load_select_unchecked(_1: &[T], _2: core_simd::masks::Mask<<T as core_simd::vector::SimdElement>::Mask, N>, _3: core_simd::vector::Simd<T, N>) -> core_simd::vector::Simd<T, N> {\n    let mut _0: core_simd::vector::Simd<T, N>;\n    let  _4: *const T;\n    debug slice => _1;\n    debug enable => _2;\n    debug or => _3;\n    debug ptr => _4;\n    bb0: {\n        _4 = slice::<impl [T]>::as_ptr(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _0 = core_simd::vector::Simd::<T, N>::load_select_ptr(_4, _2, _3) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        return;\n    }\n}\n",
  "doc": " Reads contiguous elements from `slice`. Each element is read from memory if its\n corresponding element in `enable` is `true`.\n\n When the element is disabled, that memory location is not accessed and the corresponding\n value from `or` is passed through.\n\n # Safety\n Enabled loads must not exceed the length of `slice`.\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
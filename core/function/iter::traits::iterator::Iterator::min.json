{
  "name": "iter::traits::iterator::Iterator::min",
  "safe": true,
  "callees": {
    "iter::traits::iterator::Iterator::min_by": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Returns the element that gives the minimum value with respect to the\n specified comparison function.\n\n If several elements are equally minimum, the first element is\n returned. If the iterator is empty, [`None`] is returned.\n\n # Examples\n\n ```\n let a = [-3_i32, 0, 1, 5, -10];\n assert_eq!(a.into_iter().min_by(|x, y| x.cmp(y)).unwrap(), -10);\n ```\n",
      "adt": {
        "option::Option": "Constructor"
      }
    },
    "cmp::Ord::cmp": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " This method returns an [`Ordering`] between `self` and `other`.\n\n By convention, `self.cmp(&other)` returns the ordering matching the expression\n `self <operator> other` if true.\n\n # Examples\n\n ```\n use std::cmp::Ordering;\n\n assert_eq!(5.cmp(&10), Ordering::Less);\n assert_eq!(10.cmp(&5), Ordering::Greater);\n assert_eq!(5.cmp(&5), Ordering::Equal);\n ```\n",
      "adt": {}
    }
  },
  "adts": {
    "option::Option": [
      "Plain"
    ]
  },
  "path": "iter::traits::iterator::Iterator::min",
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/iter/traits/iterator.rs:3199:5: 3205:6",
  "src": "fn min(self) -> Option<Self::Item>\n    where\n        Self: Sized,\n        Self::Item: Ord,\n    {\n        self.min_by(Ord::cmp)\n    }",
  "mir": "fn iter::traits::iterator::Iterator::min(_1: Self) -> option::Option<<Self as iter::traits::iterator::Iterator>::Item> {\n    let mut _0: option::Option<<Self as iter::traits::iterator::Iterator>::Item>;\n    debug self => _1;\n    bb0: {\n        _0 = <Self as iter::traits::iterator::Iterator>::min_by::<for<'a, 'b> fn(&'a <Self as iter::traits::iterator::Iterator>::Item, &'b <Self as iter::traits::iterator::Iterator>::Item) -> cmp::Ordering {<<Self as iter::traits::iterator::Iterator>::Item as cmp::Ord>::cmp}>(_1, <<Self as iter::traits::iterator::Iterator>::Item as cmp::Ord>::cmp) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        return;\n    }\n}\n",
  "doc": " Returns the minimum element of an iterator.\n\n If several elements are equally minimum, the first element is returned.\n If the iterator is empty, [`None`] is returned.\n\n Note that [`f32`]/[`f64`] doesn't implement [`Ord`] due to NaN being\n incomparable. You can work around this by using [`Iterator::reduce`]:\n ```\n assert_eq!(\n     [2.4, f32::NAN, 1.3]\n         .into_iter()\n         .reduce(f32::min)\n         .unwrap_or(0.),\n     1.3\n );\n ```\n\n # Examples\n\n ```\n let a = [1, 2, 3];\n let b: [u32; 0] = [];\n\n assert_eq!(a.into_iter().min(), Some(1));\n assert_eq!(b.into_iter().min(), None);\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
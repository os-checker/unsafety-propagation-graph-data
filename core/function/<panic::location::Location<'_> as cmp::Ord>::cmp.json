{
  "name": "<panic::location::Location<'_> as cmp::Ord>::cmp",
  "safe": true,
  "callees": {
    "panic::location::Location::<'a>::file": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Returns the name of the source file from which the panic originated.\n\n # `&str`, not `&Path`\n\n The returned name refers to a source path on the compiling system, but it isn't valid to\n represent this directly as a `&Path`. The compiled code may run on a different system with\n a different `Path` implementation than the system providing the contents and this library\n does not currently have a different \"host path\" type.\n\n The most surprising behavior occurs when \"the same\" file is reachable via multiple paths in\n the module system (usually using the `#[path = \"...\"]` attribute or similar), which can\n cause what appears to be identical code to return differing values from this function.\n\n # Cross-compilation\n\n This value is not suitable for passing to `Path::new` or similar constructors when the host\n platform and target platform differ.\n\n # Examples\n\n ```should_panic\n use std::panic;\n\n panic::set_hook(Box::new(|panic_info| {\n     if let Some(location) = panic_info.location() {\n         println!(\"panic occurred in file '{}'\", location.file());\n     } else {\n         println!(\"panic occurred but can't get location information...\");\n     }\n }));\n\n panic!(\"Normal panic\");\n ```\n",
      "adt": {
        "panic::location::Location": "ImmutableAsArgument"
      }
    },
    "cmp::Ord::cmp": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " This method returns an [`Ordering`] between `self` and `other`.\n\n By convention, `self.cmp(&other)` returns the ordering matching the expression\n `self <operator> other` if true.\n\n # Examples\n\n ```\n use std::cmp::Ordering;\n\n assert_eq!(5.cmp(&10), Ordering::Less);\n assert_eq!(10.cmp(&5), Ordering::Greater);\n assert_eq!(5.cmp(&5), Ordering::Equal);\n ```\n",
      "adt": {}
    },
    "cmp::Ordering::then_with": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Chains the ordering with the given function.\n\n Returns `self` when it's not `Equal`. Otherwise calls `f` and returns\n the result.\n\n # Examples\n\n ```\n use std::cmp::Ordering;\n\n let result = Ordering::Equal.then_with(|| Ordering::Less);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Less.then_with(|| Ordering::Equal);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Less.then_with(|| Ordering::Greater);\n assert_eq!(result, Ordering::Less);\n\n let result = Ordering::Equal.then_with(|| Ordering::Equal);\n assert_eq!(result, Ordering::Equal);\n\n let x: (i64, i64, i64) = (1, 2, 7);\n let y: (i64, i64, i64) = (1, 5, 3);\n let result = x.0.cmp(&y.0).then_with(|| x.1.cmp(&y.1)).then_with(|| x.2.cmp(&y.2));\n\n assert_eq!(result, Ordering::Less);\n ```\n",
      "adt": {
        "cmp::Ordering": "Constructor"
      }
    }
  },
  "adts": {
    "panic::location::Location": [
      "Ref"
    ],
    "cmp::Ordering": [
      "Plain"
    ]
  },
  "path": 15773,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:63:5: 68:6",
  "src": "fn cmp(&self, other: &Self) -> Ordering {\n        self.file()\n            .cmp(other.file())\n            .then_with(|| self.line.cmp(&other.line))\n            .then_with(|| self.col.cmp(&other.col))\n    }",
  "mir": "fn <panic::location::Location<'_> as cmp::Ord>::cmp(_1: &panic::location::Location<'_>, _2: &panic::location::Location<'_>) -> cmp::Ordering {\n    let mut _0: cmp::Ordering;\n    let mut _3: cmp::Ordering;\n    let mut _4: cmp::Ordering;\n    let  _5: &str;\n    let  _6: &str;\n    let mut _7: {closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:66:24: 66:26};\n    let mut _8: {closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:67:24: 67:26};\n    debug self => _1;\n    debug other => _2;\n    bb0: {\n        StorageLive(_3);\n        StorageLive(_4);\n        _5 = panic::location::Location::<'_>::file(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _6 = panic::location::Location::<'_>::file(_2) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        _4 = <str as cmp::Ord>::cmp(_5, _6) -> [return: bb3, unwind unreachable];\n    }\n    bb3: {\n        StorageLive(_7);\n        _7 = {closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:66:24: 66:26}(_1, _2);\n        _3 = cmp::Ordering::then_with::<{closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:66:24: 66:26}>(move _4, move _7) -> [return: bb4, unwind unreachable];\n    }\n    bb4: {\n        StorageDead(_7);\n        StorageDead(_4);\n        StorageLive(_8);\n        _8 = {closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:67:24: 67:26}(_1, _2);\n        _0 = cmp::Ordering::then_with::<{closure@/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/panic/location.rs:67:24: 67:26}>(move _3, move _8) -> [return: bb5, unwind unreachable];\n    }\n    bb5: {\n        StorageDead(_8);\n        StorageDead(_3);\n        return;\n    }\n}\n",
  "doc": "",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
{
  "name": "core::<index::Clamp<ops::range::RangeInclusive<usize>> as slice::index::SliceIndex<[T]>>::index_mut",
  "span": "$library/core/src/index.rs:248:5: 248:61",
  "src": "fn index_mut(self, slice: &mut [T]) -> &mut Self::Output {\n        let start = cmp::min(self.0.start, slice.len() - 1);\n        let end = cmp::min(self.0.end, slice.len() - 1);\n        (start..=end).index_mut(slice)\n    }"
}
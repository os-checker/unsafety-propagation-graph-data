{
  "name": "core::<wtf8::Wtf8 as ops::index::Index<ops::range::RangeFrom<usize>>>::index",
  "span": "$library/core/src/wtf8.rs:389:5: 389:59",
  "src": "fn index(&self, range: ops::RangeFrom<usize>) -> &Wtf8 {\n        if self.is_code_point_boundary(range.start) {\n            // SAFETY: is_code_point_boundary checks that the index is valid\n            unsafe { slice_unchecked(self, range.start, self.len()) }\n        } else {\n            slice_error_fail(self, range.start, self.len())\n        }\n    }"
}
{
  "name": "core::<index::Clamp<range::RangeToInclusive<usize>> as slice::index::SliceIndex<[T]>>::get_unchecked_mut",
  "span": "$library/core/src/index.rs:365:5: 365:76",
  "src": "unsafe fn get_unchecked_mut(self, slice: *mut [T]) -> *mut Self::Output {\n        // SAFETY: the caller ensures that the slice isn't empty\n        unsafe { (..=cmp::min(self.0.last, slice.len() - 1)).get_unchecked_mut(slice) }\n    }"
}
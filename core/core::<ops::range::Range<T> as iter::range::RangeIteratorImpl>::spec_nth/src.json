{
  "name": "core::<ops::range::Range<T> as iter::range::RangeIteratorImpl>::spec_nth",
  "span": "$library/core/src/iter/range.rs:776:5: 776:50",
  "src": "fn spec_nth(&mut self, n: usize) -> Option<T> {\n        if let Some(plus_n) = Step::forward_checked(self.start, n) {\n            if plus_n < self.end {\n                // SAFETY: just checked precondition\n                self.start = unsafe { Step::forward_unchecked(plus_n, 1) };\n                return Some(plus_n);\n            }\n        }\n\n        self.start = self.end;\n        None\n    }"
}
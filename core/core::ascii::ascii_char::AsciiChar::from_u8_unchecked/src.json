{
  "name": "core::ascii::ascii_char::AsciiChar::from_u8_unchecked",
  "span": "$library/core/src/ascii/ascii_char.rs:478:5: 478:57",
  "src": "pub const unsafe fn from_u8_unchecked(b: u8) -> Self {\n        // SAFETY: Our safety precondition is that `b` is in-range.\n        unsafe { transmute(b) }\n    }"
}
{
  "name": "core::num::bignum::tests::Big8x3::add",
  "span": "$library/core/src/num/bignum.rs:145:13: 145:73",
  "src": "pub fn add<'a>(&'a mut self, other: &$name) -> &'a mut $name {\n                use crate::{cmp, iter};\n\n                let mut sz = cmp::max(self.size, other.size);\n                let mut carry = false;\n                for (a, b) in iter::zip(&mut self.base[..sz], &other.base[..sz]) {\n                    let (v, c) = (*a).carrying_add(*b, carry);\n                    *a = v;\n                    carry = c;\n                }\n                if carry {\n                    self.base[sz] = 1;\n                    sz += 1;\n                }\n                self.size = sz;\n                self\n            }"
}
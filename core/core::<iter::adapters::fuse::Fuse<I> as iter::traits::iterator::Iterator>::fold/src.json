{
  "name": "core::<iter::adapters::fuse::Fuse<I> as iter::traits::iterator::Iterator>::fold",
  "span": "$library/core/src/iter/adapters/fuse.rs:93:5: 95:45",
  "src": "fn fold<Acc, Fold>(self, mut acc: Acc, fold: Fold) -> Acc\n    where\n        Fold: FnMut(Acc, Self::Item) -> Acc,\n    {\n        if let Some(iter) = self.iter {\n            acc = iter.fold(acc, fold);\n        }\n        acc\n    }"
}
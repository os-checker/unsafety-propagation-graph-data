{
  "name": "core::<iter::adapters::enumerate::Enumerate<I> as iter::traits::double_ended::DoubleEndedIterator>::try_rfold::enumerate",
  "span": "$library/core/src/iter/adapters/enumerate.rs:206:9: 209:37",
  "src": "fn enumerate<T, Acc, R>(\n            mut count: usize,\n            mut fold: impl FnMut(Acc, (usize, T)) -> R,\n        ) -> impl FnMut(Acc, T) -> R {\n            move |acc, item| {\n                count -= 1;\n                fold(acc, (count, item))\n            }\n        }"
}
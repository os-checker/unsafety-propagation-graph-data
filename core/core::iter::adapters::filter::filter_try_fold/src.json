{
  "name": "core::iter::adapters::filter::filter_try_fold",
  "span": "$library/core/src/iter/adapters/filter.rs:82:1: 85:34",
  "src": "fn filter_try_fold<'a, T, Acc, R: Try<Output = Acc>>(\n    predicate: &'a mut impl FnMut(&T) -> bool,\n    mut fold: impl FnMut(Acc, T) -> R + 'a,\n) -> impl FnMut(Acc, T) -> R + 'a {\n    move |acc, item| if predicate(&item) { fold(acc, item) } else { try { acc } }\n}"
}
{
  "name": "core::<slice::sort::shared::smallsort::CopyOnDrop<T> as ops::drop::Drop>::drop",
  "span": "$library/core/src/slice/sort/shared/smallsort.rs:302:5: 302:23",
  "src": "fn drop(&mut self) {\n        // SAFETY: `src` must contain `len` initialized elements, and dst must\n        // be valid to write `len` elements.\n        unsafe {\n            ptr::copy_nonoverlapping(self.src, self.dst, self.len);\n        }\n    }"
}
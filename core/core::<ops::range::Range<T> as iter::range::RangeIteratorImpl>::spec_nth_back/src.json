{
  "name": "core::<ops::range::Range<T> as iter::range::RangeIteratorImpl>::spec_nth_back",
  "span": "$library/core/src/iter/range.rs:817:5: 817:55",
  "src": "fn spec_nth_back(&mut self, n: usize) -> Option<T> {\n        if let Some(minus_n) = Step::backward_checked(self.end, n) {\n            if minus_n > self.start {\n                // SAFETY: just checked precondition\n                self.end = unsafe { Step::backward_unchecked(minus_n, 1) };\n                return Some(self.end);\n            }\n        }\n\n        self.end = self.start;\n        None\n    }"
}
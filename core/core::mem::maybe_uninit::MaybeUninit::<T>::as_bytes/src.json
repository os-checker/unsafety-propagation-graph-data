{
  "name": "core::mem::maybe_uninit::MaybeUninit::<T>::as_bytes",
  "span": "$library/core/src/mem/maybe_uninit.rs:1060:5: 1060:55",
  "src": "pub const fn as_bytes(&self) -> &[MaybeUninit<u8>] {\n        // SAFETY: MaybeUninit<u8> is always valid, even for padding bytes\n        unsafe {\n            slice::from_raw_parts(self.as_ptr().cast::<MaybeUninit<u8>>(), super::size_of::<T>())\n        }\n    }"
}
{
  "name": "core::<index::Clamp<range::RangeInclusive<usize>> as slice::index::SliceIndex<[T]>>::get_mut",
  "span": "$library/core/src/index.rs:179:5: 179:67",
  "src": "fn get_mut(self, slice: &mut [T]) -> Option<&mut Self::Output> {\n        let start = cmp::min(self.0.start, slice.len() - 1);\n        let end = cmp::min(self.0.last, slice.len() - 1);\n        (start..=end).get_mut(slice)\n    }"
}
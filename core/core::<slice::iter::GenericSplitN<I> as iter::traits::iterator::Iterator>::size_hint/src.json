{
  "name": "core::<slice::iter::GenericSplitN<I> as iter::traits::iterator::Iterator>::size_hint",
  "span": "$library/core/src/slice/iter.rs:1129:5: 1129:50",
  "src": "fn size_hint(&self) -> (usize, Option<usize>) {\n        let (lower, upper_opt) = self.iter.size_hint();\n        (\n            cmp::min(self.count, lower),\n            Some(upper_opt.map_or(self.count, |upper| cmp::min(self.count, upper))),\n        )\n    }"
}
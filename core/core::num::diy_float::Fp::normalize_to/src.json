{
  "name": "core::num::diy_float::Fp::normalize_to",
  "span": "$library/core/src/num/diy_float.rs:42:5: 42:46",
  "src": "pub fn normalize_to(self, e: i16) -> Self {\n        let edelta = self.e - e;\n        assert!(edelta >= 0);\n        let edelta = edelta as usize;\n        assert_eq!(self.f << edelta >> edelta, self.f);\n        Self { f: self.f << edelta, e }\n    }"
}
{
  "name": "core::<slice::iter::RChunks<'a, T> as iter::traits::iterator::Iterator>::nth",
  "span": "$library/core/src/slice/iter.rs:2335:5: 2335:54",
  "src": "fn nth(&mut self, n: usize) -> Option<Self::Item> {\n        if let Some(end) = n.checked_mul(self.chunk_size)\n            && end < self.v.len()\n        {\n            let end = self.v.len() - end;\n            let rest = &self.v[..end];\n            let (rest, chunk) = rest.split_at(end.saturating_sub(self.chunk_size));\n            self.v = rest;\n            Some(chunk)\n        } else {\n            self.v = &self.v[..0]; // cheaper than &[]\n            None\n        }\n    }"
}
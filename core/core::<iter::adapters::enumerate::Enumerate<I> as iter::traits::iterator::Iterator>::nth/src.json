{
  "name": "core::<iter::adapters::enumerate::Enumerate<I> as iter::traits::iterator::Iterator>::nth",
  "span": "$library/core/src/iter/adapters/enumerate.rs:93:5: 93:60",
  "src": "fn nth(&mut self, n: usize) -> Option<(usize, I::Item)> {\n        let a = self.iter.nth(n)?;\n        let i = self.count + n;\n        self.count = i + 1;\n        Some((i, a))\n    }"
}
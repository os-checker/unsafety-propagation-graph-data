{
  "name": "core::<panic::location::Location<'_> as hash::Hash>::hash",
  "span": "$library/core/src/panic/location.rs:80:5: 80:45",
  "src": "fn hash<H: Hasher>(&self, state: &mut H) {\n        self.file().hash(state);\n        self.line.hash(state);\n        self.col.hash(state);\n    }"
}
{
  "name": "core::<slice::iter::RChunksExactMut<'a, T> as iter::traits::iterator::Iterator>::nth",
  "span": "$library/core/src/slice/iter.rs:2868:5: 2868:55",
  "src": "fn nth(&mut self, n: usize) -> Option<&'a mut [T]> {\n        if let Some(end) = n.checked_mul(self.chunk_size)\n            && end < self.v.len()\n        {\n            let idx = self.v.len() - end;\n            // SAFETY: The self.v contract ensures that any split_at_mut is valid.\n            let (fst, _) = unsafe { self.v.split_at_mut(idx) };\n            self.v = fst;\n            self.next()\n        } else {\n            self.v = &mut [];\n            None\n        }\n    }"
}
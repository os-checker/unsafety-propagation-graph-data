{
  "name": "core::<index::Clamp<ops::range::RangeToInclusive<usize>> as slice::index::SliceIndex<[T]>>::get_unchecked",
  "span": "$library/core/src/index.rs:391:5: 391:76",
  "src": "unsafe fn get_unchecked(self, slice: *const [T]) -> *const Self::Output {\n        // SAFETY: the caller ensures that the slice isn't empty\n        unsafe { (..=cmp::min(self.0.end, slice.len() - 1)).get_unchecked(slice) }\n    }"
}
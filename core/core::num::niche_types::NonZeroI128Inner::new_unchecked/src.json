{
  "name": "core::num::niche_types::NonZeroI128Inner::new_unchecked",
  "span": "$library/core/src/num/niche_types.rs:52:13: 52:65",
  "src": "pub const unsafe fn new_unchecked(val: $int) -> Self {\n                // SAFETY: Caller promised that `val` is within the valid range.\n                unsafe { $name(val) }\n            }"
}
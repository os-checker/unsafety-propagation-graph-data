{
  "name": "std::sys::net::connection::socket::unix::on_resolver_failure",
  "span": "$library/std/src/sys/net/connection/socket/unix.rs:682:1: 682:25",
  "src": "fn on_resolver_failure() {\n    use crate::sys;\n\n    // If the version fails to parse, we treat it the same as \"not glibc\".\n    if let Some(version) = sys::os::glibc_version() {\n        if version < (2, 26) {\n            unsafe { libc::res_init() };\n        }\n    }\n}"
}
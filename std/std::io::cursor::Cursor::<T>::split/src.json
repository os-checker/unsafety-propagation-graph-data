{
  "name": "std::io::cursor::Cursor::<T>::split",
  "span": "$library/std/src/io/cursor.rs:233:5: 233:42",
  "src": "pub fn split(&self) -> (&[u8], &[u8]) {\n        let slice = self.inner.as_ref();\n        let pos = self.pos.min(slice.len() as u64);\n        slice.split_at(pos as usize)\n    }"
}
{
  "name": "std::os::unix::net::stream::UnixStream::connect_addr",
  "span": "$library/std/src/os/unix/net/stream.rs:140:5: 140:76",
  "src": "pub fn connect_addr(socket_addr: &SocketAddr) -> io::Result<UnixStream> {\n        unsafe {\n            let inner = Socket::new(libc::AF_UNIX, libc::SOCK_STREAM)?;\n            cvt(libc::connect(\n                inner.as_raw_fd(),\n                (&raw const socket_addr.addr) as *const _,\n                socket_addr.len,\n            ))?;\n            Ok(UnixStream(inner))\n        }\n    }"
}
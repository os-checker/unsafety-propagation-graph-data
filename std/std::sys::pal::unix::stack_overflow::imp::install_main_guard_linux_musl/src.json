{
  "name": "std::sys::pal::unix::stack_overflow::imp::install_main_guard_linux_musl",
  "span": "$library/std/src/sys/pal/unix/stack_overflow.rs:448:5: 448:87",
  "src": "unsafe fn install_main_guard_linux_musl(_page_size: usize) -> Option<Range<usize>> {\n        // For the main thread, the musl's pthread_attr_getstack\n        // returns the current stack size, rather than maximum size\n        // it can eventually grow to. It cannot be used to determine\n        // the position of kernel's stack guard.\n        None\n    }"
}
{
  "name": "collections::hash::set::HashSet::<T, S>::difference",
  "safe": true,
  "callees": {
    "collections::hash::set::HashSet::<T, S>::iter": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " An iterator visiting all elements in arbitrary order.\n The iterator element type is `&'a T`.\n\n # Examples\n\n ```\n use std::collections::HashSet;\n let mut set = HashSet::new();\n set.insert(\"a\");\n set.insert(\"b\");\n\n // Will print in an arbitrary order.\n for x in set.iter() {\n     println!(\"{x}\");\n }\n ```\n\n # Performance\n\n In the current implementation, iterating over set takes O(capacity) time\n instead of O(len) because it internally visits empty buckets too.\n",
      "adt": {
        "collections::hash::set::HashSet": "ImmutableAsArgument",
        "collections::hash::set::Iter": "Constructor"
      }
    }
  },
  "adts": {
    "collections::hash::set::HashSet": [
      "Ref"
    ],
    "collections::hash::set::Iter": [
      "Plain"
    ],
    "collections::hash::set::Difference": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "std::collections::hash::set::HashSet::<T, S>::difference"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/collections/hash/set.rs:572:5: 574:6",
  "src": "pub fn difference<'a>(&'a self, other: &'a HashSet<T, S>) -> Difference<'a, T, S> {\n        Difference { iter: self.iter(), other }\n    }",
  "mir": "fn collections::hash::set::HashSet::<T, S>::difference(_1: &collections::hash::set::HashSet<T, S>, _2: &collections::hash::set::HashSet<T, S>) -> collections::hash::set::Difference<'_, T, S> {\n    let mut _0: collections::hash::set::Difference<'_, T, S>;\n    let mut _3: collections::hash::set::Iter<'_, T>;\n    debug self => _1;\n    debug other => _2;\n    bb0: {\n        StorageLive(_3);\n        _3 = collections::hash::set::HashSet::<T, S>::iter(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _0 = Difference(move _3, _2);\n        StorageDead(_3);\n        return;\n    }\n}\n",
  "doc": " Visits the values representing the difference,\n i.e., the values that are in `self` but not in `other`.\n\n # Examples\n\n ```\n use std::collections::HashSet;\n let a = HashSet::from([1, 2, 3]);\n let b = HashSet::from([4, 2, 3, 4]);\n\n // Can be seen as `a - b`.\n for x in a.difference(&b) {\n     println!(\"{x}\"); // Print 1\n }\n\n let diff: HashSet<_> = a.difference(&b).collect();\n assert_eq!(diff, [1].iter().collect());\n\n // Note that difference is not symmetric,\n // and `b - a` means something else:\n let diff: HashSet<_> = b.difference(&a).collect();\n assert_eq!(diff, [4].iter().collect());\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
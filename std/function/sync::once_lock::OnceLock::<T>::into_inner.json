{
  "name": "sync::once_lock::OnceLock::<T>::into_inner",
  "safe": true,
  "callees": {
    "sync::once_lock::OnceLock::<T>::take": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Takes the value out of this `OnceLock`, moving it back to an uninitialized state.\n\n Has no effect and returns `None` if the `OnceLock` was uninitialized.\n\n Since this method borrows the `OnceLock` mutably, it is statically guaranteed that\n no active borrows to the `OnceLock` exist, including from other threads.\n\n # Examples\n\n ```\n use std::sync::OnceLock;\n\n let mut cell: OnceLock<String> = OnceLock::new();\n assert_eq!(cell.take(), None);\n\n let mut cell = OnceLock::new();\n cell.set(\"hello\".to_string()).unwrap();\n assert_eq!(cell.take(), Some(\"hello\".to_string()));\n assert_eq!(cell.get(), None);\n ```\n",
      "adt": {
        "sync::once_lock::OnceLock": "MutableAsArgument",
        "core::option::Option": "Constructor"
      }
    }
  },
  "adts": {
    "sync::once_lock::OnceLock": [
      "MutRef",
      "Plain"
    ],
    "core::option::Option": [
      "Plain"
    ]
  },
  "path": 3180,
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/sync/once_lock.rs:479:5: 481:6",
  "src": "pub fn into_inner(mut self) -> Option<T> {\n        self.take()\n    }",
  "mir": "fn sync::once_lock::OnceLock::<T>::into_inner(_1: sync::once_lock::OnceLock<T>) -> core::option::Option<T> {\n    let mut _0: core::option::Option<T>;\n    let mut _2: &mut sync::once_lock::OnceLock<T>;\n    debug self => _1;\n    bb0: {\n        StorageLive(_2);\n        _2 = &mut _1;\n        _0 = sync::once_lock::OnceLock::<T>::take(move _2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        StorageDead(_2);\n        drop(_1) -> [return: bb2, unwind unreachable];\n    }\n    bb2: {\n        return;\n    }\n}\n",
  "doc": " Consumes the `OnceLock`, returning the wrapped value. Returns\n `None` if the cell was uninitialized.\n\n # Examples\n\n ```\n use std::sync::OnceLock;\n\n let cell: OnceLock<String> = OnceLock::new();\n assert_eq!(cell.into_inner(), None);\n\n let cell = OnceLock::new();\n cell.set(\"hello\".to_string()).unwrap();\n assert_eq!(cell.into_inner(), Some(\"hello\".to_string()));\n ```\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
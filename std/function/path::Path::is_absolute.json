{
  "name": "path::Path::is_absolute",
  "safe": true,
  "callees": {
    "sys::path::unix::is_absolute": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": "",
      "adt": {
        "path::Path": "ImmutableAsArgument"
      }
    }
  },
  "adts": {
    "path::Path": [
      "Ref"
    ]
  },
  "path": {
    "type": "Local",
    "path": "std::path::Path::is_absolute"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/path.rs:2510:5: 2512:6",
  "src": "pub fn is_absolute(&self) -> bool {\n        sys::path::is_absolute(self)\n    }",
  "mir": "fn path::Path::is_absolute(_1: &path::Path) -> bool {\n    let mut _0: bool;\n    debug self => _1;\n    bb0: {\n        _0 = sys::path::unix::is_absolute(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        return;\n    }\n}\n",
  "doc": " Returns `true` if the `Path` is absolute, i.e., if it is independent of\n the current directory.\n\n * On Unix, a path is absolute if it starts with the root, so\n `is_absolute` and [`has_root`] are equivalent.\n\n * On Windows, a path is absolute if it has a prefix and starts with the\n root: `c:\\windows` is absolute, while `c:temp` and `\\temp` are not.\n\n # Examples\n\n ```\n use std::path::Path;\n\n assert!(!Path::new(\"foo.txt\").is_absolute());\n ```\n\n [`has_root`]: Path::has_root\n",
  "tags": {
    "tags": [
      {
        "tag": {
          "typ": null,
          "name": "deprecated"
        },
        "args": []
      }
    ],
    "spec": {},
    "docs": [
      "* deprecated\n"
    ]
  }
}
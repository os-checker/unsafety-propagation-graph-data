{
  "name": "path::Path::to_string_lossy",
  "safe": true,
  "callees": {
    "ffi::os_str::OsStr::to_string_lossy": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Converts an `OsStr` to a <code>[Cow]<[str]></code>.\n\n Any non-UTF-8 sequences are replaced with\n [`U+FFFD REPLACEMENT CHARACTER`][U+FFFD].\n\n [U+FFFD]: crate::char::REPLACEMENT_CHARACTER\n\n # Examples\n\n Calling `to_string_lossy` on an `OsStr` with invalid unicode:\n\n ```\n // Note, due to differences in how Unix and Windows represent strings,\n // we are forced to complicate this example, setting up example `OsStr`s\n // with different source data and via different platform extensions.\n // Understand that in reality you could end up with such example invalid\n // sequences simply through collecting user command line arguments, for\n // example.\n\n #[cfg(unix)] {\n     use std::ffi::OsStr;\n     use std::os::unix::ffi::OsStrExt;\n\n     // Here, the values 0x66 and 0x6f correspond to 'f' and 'o'\n     // respectively. The value 0x80 is a lone continuation byte, invalid\n     // in a UTF-8 sequence.\n     let source = [0x66, 0x6f, 0x80, 0x6f];\n     let os_str = OsStr::from_bytes(&source[..]);\n\n     assert_eq!(os_str.to_string_lossy(), \"fo�o\");\n }\n #[cfg(windows)] {\n     use std::ffi::OsString;\n     use std::os::windows::prelude::*;\n\n     // Here the values 0x0066 and 0x006f correspond to 'f' and 'o'\n     // respectively. The value 0xD800 is a lone surrogate half, invalid\n     // in a UTF-16 sequence.\n     let source = [0x0066, 0x006f, 0xD800, 0x006f];\n     let os_string = OsString::from_wide(&source[..]);\n     let os_str = os_string.as_os_str();\n\n     assert_eq!(os_str.to_string_lossy(), \"fo�o\");\n }\n ```\n",
      "adt": {
        "ffi::os_str::OsStr": "ImmutableAsArgument",
        "alloc_crate::borrow::Cow": "Constructor"
      }
    }
  },
  "adts": {
    "ffi::os_str::OsStr": [
      "Ref"
    ],
    "path::Path": [
      "DerefVariantField(VariantIdx(None)-FieldIdx(Some(0)))",
      "Ref"
    ],
    "alloc_crate::borrow::Cow": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "std::path::Path::to_string_lossy"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/path.rs:2466:5: 2468:6",
  "src": "pub fn to_string_lossy(&self) -> Cow<'_, str> {\n        self.inner.to_string_lossy()\n    }",
  "mir": "fn path::Path::to_string_lossy(_1: &path::Path) -> alloc_crate::borrow::Cow<'_, str> {\n    let mut _0: alloc_crate::borrow::Cow<'_, str>;\n    let mut _2: &ffi::os_str::OsStr;\n    debug self => _1;\n    bb0: {\n        StorageLive(_2);\n        _2 = &((*_1).0: ffi::os_str::OsStr);\n        _0 = ffi::os_str::OsStr::to_string_lossy(move _2) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        StorageDead(_2);\n        return;\n    }\n}\n",
  "doc": " Converts a `Path` to a [`Cow<str>`].\n\n Any non-UTF-8 sequences are replaced with\n [`U+FFFD REPLACEMENT CHARACTER`][U+FFFD].\n\n [U+FFFD]: super::char::REPLACEMENT_CHARACTER\n\n # Examples\n\n Calling `to_string_lossy` on a `Path` with valid unicode:\n\n ```\n use std::path::Path;\n\n let path = Path::new(\"foo.txt\");\n assert_eq!(path.to_string_lossy(), \"foo.txt\");\n ```\n\n Had `path` contained invalid unicode, the `to_string_lossy` call might\n have returned `\"fo�.txt\"`.\n",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
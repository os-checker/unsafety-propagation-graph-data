{
  "name": "thread::lifecycle::spawn_unchecked::MaybeDangling::<T>::new",
  "safe": true,
  "callees": {
    "core::mem::MaybeUninit::<T>::new": {
      "safe": true,
      "tags": {
        "tags": [],
        "spec": {},
        "docs": []
      },
      "doc": " Creates a new `MaybeUninit<T>` initialized with the given value.\n It is safe to call [`assume_init`] on the return value of this function.\n\n Note that dropping a `MaybeUninit<T>` will never call `T`'s drop code.\n It is your responsibility to make sure `T` gets dropped if it got initialized.\n\n # Example\n\n ```\n use std::mem::MaybeUninit;\n\n let v: MaybeUninit<Vec<u8>> = MaybeUninit::new(vec![42]);\n # // Prevent leaks for Miri\n # unsafe { let _ = MaybeUninit::assume_init(v); }\n ```\n\n [`assume_init`]: MaybeUninit::assume_init\n",
      "adt": {}
    }
  },
  "adts": {
    "core::mem::MaybeUninit": [
      "Plain"
    ],
    "thread::lifecycle::spawn_unchecked::MaybeDangling": [
      "Plain"
    ]
  },
  "path": {
    "type": "Local",
    "path": "std::thread::lifecycle::spawn_unchecked::MaybeDangling::<T>::new"
  },
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/thread/lifecycle.rs:67:9: 69:10",
  "src": "fn new(x: T) -> Self {\n            MaybeDangling(MaybeUninit::new(x))\n        }",
  "mir": "fn thread::lifecycle::spawn_unchecked::MaybeDangling::<T>::new(_1: T) -> thread::lifecycle::spawn_unchecked::MaybeDangling<T> {\n    let mut _0: thread::lifecycle::spawn_unchecked::MaybeDangling<T>;\n    let mut _2: core::mem::MaybeUninit<T>;\n    debug x => _1;\n    bb0: {\n        StorageLive(_2);\n        _2 = core::mem::MaybeUninit::<T>::new(_1) -> [return: bb1, unwind unreachable];\n    }\n    bb1: {\n        _0 = MaybeDangling(move _2);\n        StorageDead(_2);\n        return;\n    }\n}\n",
  "doc": "",
  "tags": {
    "tags": [],
    "spec": {},
    "docs": []
  }
}
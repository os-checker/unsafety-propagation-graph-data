{
  "name": "std::sys::sync::rwlock::futex::RwLock::spin_until",
  "span": "$library/std/src/sys/sync/rwlock/futex.rs:334:5: 334:69",
  "src": "fn spin_until(&self, f: impl Fn(Primitive) -> bool) -> Primitive {\n        let mut spin = 100; // Chosen by fair dice roll.\n        loop {\n            let state = self.state.load(Relaxed);\n            if f(state) || spin == 0 {\n                return state;\n            }\n            crate::hint::spin_loop();\n            spin -= 1;\n        }\n    }"
}
{
  "name": "std::panicking::panic_count::decrease",
  "span": "$library/std/src/panicking.rs:442:5: 442:22",
  "src": "pub fn decrease() {\n        GLOBAL_PANIC_COUNT.fetch_sub(1, Ordering::Relaxed);\n        LOCAL_PANIC_COUNT.with(|c| {\n            let (count, _) = c.get();\n            c.set((count - 1, false));\n        });\n    }"
}
{
  "name": "std::sys::fd::unix::FileDesc::write_vectored",
  "span": "$library/std/src/sys/fd/unix.rs:361:5: 361:76",
  "src": "pub fn write_vectored(&self, bufs: &[IoSlice<'_>]) -> io::Result<usize> {\n        let ret = cvt(unsafe {\n            libc::writev(\n                self.as_raw_fd(),\n                bufs.as_ptr() as *const libc::iovec,\n                cmp::min(bufs.len(), max_iov()) as libc::c_int,\n            )\n        })?;\n        Ok(ret as usize)\n    }"
}
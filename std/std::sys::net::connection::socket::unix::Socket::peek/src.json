{
  "name": "std::sys::net::connection::socket::unix::Socket::peek",
  "span": "$library/std/src/sys/net/connection/socket/unix.rs:307:5: 307:60",
  "src": "pub fn peek(&self, buf: &mut [u8]) -> io::Result<usize> {\n        let mut buf = BorrowedBuf::from(buf);\n        self.recv_with_flags(buf.unfilled(), MSG_PEEK)?;\n        Ok(buf.len())\n    }"
}
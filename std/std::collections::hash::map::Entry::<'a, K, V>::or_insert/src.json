{
  "name": "std::collections::hash::map::Entry::<'a, K, V>::or_insert",
  "span": "$library/std/src/collections/hash/map.rs:2347:5: 2347:52",
  "src": "pub fn or_insert(self, default: V) -> &'a mut V {\n        match self {\n            Occupied(entry) => entry.into_mut(),\n            Vacant(entry) => entry.insert(default),\n        }\n    }"
}
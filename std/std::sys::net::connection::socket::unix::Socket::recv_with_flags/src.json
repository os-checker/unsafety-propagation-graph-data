{
  "name": "std::sys::net::connection::socket::unix::Socket::recv_with_flags",
  "span": "$library/std/src/sys/net/connection/socket/unix.rs:286:5: 286:91",
  "src": "fn recv_with_flags(&self, mut buf: BorrowedCursor<'_>, flags: c_int) -> io::Result<()> {\n        let ret = cvt(unsafe {\n            libc::recv(\n                self.as_raw_fd(),\n                buf.as_mut().as_mut_ptr() as *mut c_void,\n                buf.capacity(),\n                flags,\n            )\n        })?;\n        unsafe {\n            buf.advance(ret as usize);\n        }\n        Ok(())\n    }"
}
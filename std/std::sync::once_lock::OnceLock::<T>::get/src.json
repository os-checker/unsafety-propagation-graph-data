{
  "name": "std::sync::once_lock::OnceLock::<T>::get",
  "span": "$library/std/src/sync/once_lock.rs:154:5: 154:36",
  "src": "pub fn get(&self) -> Option<&T> {\n        if self.is_initialized() {\n            // Safe b/c checked is_initialized\n            Some(unsafe { self.get_unchecked() })\n        } else {\n            None\n        }\n    }"
}
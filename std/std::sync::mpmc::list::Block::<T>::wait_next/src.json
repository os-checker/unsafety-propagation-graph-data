{
  "name": "std::sync::mpmc::list::Block::<T>::wait_next",
  "span": "$library/std/src/sync/mpmc/list.rs:77:5: 77:41",
  "src": "fn wait_next(&self) -> *mut Block<T> {\n        let backoff = Backoff::new();\n        loop {\n            let next = self.next.load(Ordering::Acquire);\n            if !next.is_null() {\n                return next;\n            }\n            backoff.spin_heavy();\n        }\n    }"
}
{
  "name": "std::<sys::process::unix::common::cstring_array::CStringIter<'a> as core::iter::Iterator>::next",
  "span": "$library/std/src/sys/process/unix/common/cstring_array.rs:96:5: 96:43",
  "src": "fn next(&mut self) -> Option<&'a CStr> {\n        // SAFETY:\n        // `CStringArray` owns all of its strings. Also, this is not the null\n        // pointer since the last element is excluded when creating `iter`.\n        self.iter.next().map(|&p| unsafe { CStr::from_ptr(p) })\n    }"
}
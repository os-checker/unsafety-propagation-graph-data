{
  "name": "std::<io::cursor::Cursor<T> as io::Read>::read_to_end",
  "span": "$library/std/src/io/cursor.rs:381:5: 381:70",
  "src": "fn read_to_end(&mut self, buf: &mut Vec<u8>) -> io::Result<usize> {\n        let content = Cursor::split(self).1;\n        let len = content.len();\n        buf.try_reserve(len)?;\n        buf.extend_from_slice(content);\n        self.pos += len as u64;\n\n        Ok(len)\n    }"
}
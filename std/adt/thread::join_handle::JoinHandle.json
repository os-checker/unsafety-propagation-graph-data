{
  "name": "thread::join_handle::JoinHandle",
  "constructors": [
    "thread::builder::Builder::spawn",
    "thread::builder::Builder::spawn_unchecked",
    "thread::functions::spawn"
  ],
  "access_self_as_arg": {
    "read": [
      "thread::join_handle::JoinHandle::<T>::thread",
      "thread::join_handle::JoinHandle::<T>::is_finished",
      "<thread::join_handle::JoinHandle<T> as sys_common::AsInner<sys::thread::unix::Thread>>::as_inner",
      "<thread::join_handle::JoinHandle<T> as core::fmt::Debug>::fmt",
      "<thread::join_handle::JoinHandle<T> as os::unix::thread::JoinHandleExt>::as_pthread_t"
    ],
    "write": [],
    "other": [
      "thread::join_handle::JoinHandle::<T>::join",
      "<thread::join_handle::JoinHandle<T> as sys_common::IntoInner<sys::thread::unix::Thread>>::into_inner",
      "<thread::join_handle::JoinHandle<T> as os::unix::thread::JoinHandleExt>::into_pthread_t",
      "thread::join_handle::JoinHandle::<T>::join",
      "<thread::join_handle::JoinHandle<T> as sys_common::IntoInner<sys::thread::unix::Thread>>::into_inner"
    ]
  },
  "access_self_as_locals": {
    "read": [],
    "write": [],
    "other": [
      "thread::builder::Builder::spawn_unchecked",
      "thread::functions::spawn"
    ]
  },
  "access_field": [
    {
      "read": [],
      "write": [
        "thread::join_handle::JoinHandle::<T>::thread",
        "thread::join_handle::JoinHandle::<T>::is_finished",
        "<thread::join_handle::JoinHandle<T> as sys_common::AsInner<sys::thread::unix::Thread>>::as_inner"
      ],
      "other": []
    }
  ],
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/thread/join_handle.rs:72:1: 72:25",
  "src": "pub struct JoinHandle<T>",
  "kind": "Struct",
  "doc_adt": " An owned permission to join on a thread (block on its termination).\n\n A `JoinHandle` *detaches* the associated thread when it is dropped, which\n means that there is no longer any handle to the thread and no way to `join`\n on it.\n\n Due to platform restrictions, it is not possible to [`Clone`] this\n handle: the ability to join a thread is a uniquely-owned permission.\n\n This `struct` is created by the [`thread::spawn`] function and the\n [`thread::Builder::spawn`] method.\n\n # Examples\n\n Creation from [`thread::spawn`]:\n\n ```\n use std::thread;\n\n let join_handle: thread::JoinHandle<_> = thread::spawn(|| {\n     // some work here\n });\n ```\n\n Creation from [`thread::Builder::spawn`]:\n\n ```\n use std::thread;\n\n let builder = thread::Builder::new();\n\n let join_handle: thread::JoinHandle<_> = builder.spawn(|| {\n     // some work here\n }).unwrap();\n ```\n\n A thread being detached and outliving the thread that spawned it:\n\n ```no_run\n use std::thread;\n use std::time::Duration;\n\n let original_thread = thread::spawn(|| {\n     let _detached_thread = thread::spawn(|| {\n         // Here we sleep to make sure that the first thread returns before.\n         thread::sleep(Duration::from_millis(10));\n         // This will be called, even though the JoinHandle is dropped.\n         println!(\"♫ Still alive ♫\");\n     });\n });\n\n original_thread.join().expect(\"The thread being joined has panicked\");\n println!(\"Original thread is joined.\");\n\n // We make sure that the new thread has time to run, before the main\n // thread returns.\n\n thread::sleep(Duration::from_millis(1000));\n ```\n\n [`thread::Builder::spawn`]: super::Builder::spawn\n [`thread::spawn`]: super::spawn\n",
  "variant_fields": {
    "VariantIdx(None)-FieldIdx(Some(0))": {
      "name": "0",
      "doc": ""
    }
  }
}
{
  "name": "sync::mpsc::Sender",
  "constructors": [
    "sync::mpsc::channel",
    "<sync::mpsc::Sender<T> as core::clone::Clone>::clone"
  ],
  "access_self_as_arg": {
    "read": [
      "sync::mpsc::Sender::<T>::send",
      "<sync::mpsc::Sender<T> as core::clone::Clone>::clone",
      "<sync::mpsc::Sender<T> as core::fmt::Debug>::fmt"
    ],
    "write": [],
    "other": [
      "<sync::mpsc::Sender<T> as core::clone::Clone>::clone"
    ]
  },
  "access_self_as_locals": {
    "read": [],
    "write": [],
    "other": [
      "sync::mpsc::channel"
    ]
  },
  "access_field": [
    {
      "read": [],
      "write": [
        "sync::mpsc::Sender::<T>::send",
        "<sync::mpsc::Sender<T> as core::clone::Clone>::clone"
      ],
      "other": []
    }
  ],
  "span": "/home/gh-zjp-CN/.rustup/toolchains/nightly-2025-12-06-aarch64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/sync/mpsc.rs:333:1: 333:21",
  "src": "pub struct Sender<T>",
  "kind": "Struct",
  "doc_adt": " The sending-half of Rust's asynchronous [`channel`] type.\n\n Messages can be sent through this channel with [`send`].\n\n Note: all senders (the original and its clones) need to be dropped for the receiver\n to stop blocking to receive messages with [`Receiver::recv`].\n\n [`send`]: Sender::send\n\n # Examples\n\n ```rust\n use std::sync::mpsc::channel;\n use std::thread;\n\n let (sender, receiver) = channel();\n let sender2 = sender.clone();\n\n // First thread owns sender\n thread::spawn(move || {\n     sender.send(1).unwrap();\n });\n\n // Second thread owns sender2\n thread::spawn(move || {\n     sender2.send(2).unwrap();\n });\n\n let msg = receiver.recv().unwrap();\n let msg2 = receiver.recv().unwrap();\n\n assert_eq!(3, msg + msg2);\n ```\n",
  "variant_fields": {
    "VariantIdx(None)-FieldIdx(Some(0))": {
      "name": "inner",
      "doc": ""
    }
  }
}
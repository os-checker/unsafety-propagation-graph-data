{
  "name": "std::thread::thread::Thread::cname",
  "span": "$library/std/src/thread/thread.rs:307:5: 307:48",
  "src": "pub(crate) fn cname(&self) -> Option<&CStr> {\n        if let Some(name) = &self.inner.name {\n            Some(name.as_cstr())\n        } else if main_thread::get() == Some(self.inner.id) {\n            Some(c\"main\")\n        } else {\n            None\n        }\n    }"
}
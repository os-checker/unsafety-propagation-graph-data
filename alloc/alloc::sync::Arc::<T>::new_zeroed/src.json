{
  "name": "alloc::sync::Arc::<T>::new_zeroed",
  "span": "$library/alloc/src/sync.rs:543:5: 543:52",
  "src": "pub fn new_zeroed() -> Arc<mem::MaybeUninit<T>> {\n        unsafe {\n            Arc::from_ptr(Arc::allocate_for_layout(\n                Layout::new::<T>(),\n                |layout| Global.allocate_zeroed(layout),\n                <*mut u8>::cast,\n            ))\n        }\n    }"
}
{
  "name": "alloc::collections::btree::map::entry::Entry::<'a, K, V, A>::or_insert_with",
  "span": "$library/alloc/src/collections/btree/map/entry.rs:184:5: 184:75",
  "src": "pub fn or_insert_with<F: FnOnce() -> V>(self, default: F) -> &'a mut V {\n        match self {\n            Occupied(entry) => entry.into_mut(),\n            Vacant(entry) => entry.insert(default()),\n        }\n    }"
}
{
  "name": "alloc::<collections::btree::set::ExtractIf<'_, T, R, F, A> as core::iter::Iterator>::next",
  "span": "$library/alloc/src/collections/btree/set.rs:1588:5: 1588:36",
  "src": "fn next(&mut self) -> Option<T> {\n        let pred = &mut self.pred;\n        let mut mapped_pred = |k: &T, _v: &mut SetValZST| pred(k);\n        self.inner.next(&mut mapped_pred, self.alloc.clone()).map(|(k, _)| k)\n    }"
}
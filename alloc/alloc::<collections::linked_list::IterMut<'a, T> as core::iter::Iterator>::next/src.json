{
  "name": "alloc::<collections::linked_list::IterMut<'a, T> as core::iter::Iterator>::next",
  "span": "$library/alloc/src/collections/linked_list.rs:1271:5: 1271:44",
  "src": "fn next(&mut self) -> Option<&'a mut T> {\n        if self.len == 0 {\n            None\n        } else {\n            self.head.map(|node| unsafe {\n                // Need an unbound lifetime to get 'a\n                let node = &mut *node.as_ptr();\n                self.len -= 1;\n                self.head = node.next;\n                &mut node.element\n            })\n        }\n    }"
}
{
  "name": "alloc::rc::Rc::<T, A>::new_zeroed_in",
  "span": "$library/alloc/src/rc.rs:819:5: 819:65",
  "src": "pub fn new_zeroed_in(alloc: A) -> Rc<mem::MaybeUninit<T>, A> {\n        unsafe {\n            Rc::from_ptr_in(\n                Rc::allocate_for_layout(\n                    Layout::new::<T>(),\n                    |layout| alloc.allocate_zeroed(layout),\n                    <*mut u8>::cast,\n                ),\n                alloc,\n            )\n        }\n    }"
}
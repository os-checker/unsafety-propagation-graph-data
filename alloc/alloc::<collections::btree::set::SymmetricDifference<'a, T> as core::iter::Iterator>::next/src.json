{
  "name": "alloc::<collections::btree::set::SymmetricDifference<'a, T> as core::iter::Iterator>::next",
  "span": "$library/alloc/src/collections/btree/set.rs:2007:5: 2007:40",
  "src": "fn next(&mut self) -> Option<&'a T> {\n        loop {\n            let (a_next, b_next) = self.0.nexts(Self::Item::cmp);\n            if a_next.and(b_next).is_none() {\n                return a_next.or(b_next);\n            }\n        }\n    }"
}